Transcendence archived experiment summary
=========================================

Scope
- This archive holds historical experiment evidence that was previously scattered at the benchmark root.
- Intermediate artifacts such as candidate predictions, per-split CSVs, parse-smoke files, and stitched reports can be regenerated from code and are not treated as primary entry points.
- Primary retained evidence in this archive is the set of `*_summary_*.json` and `*_summary_*.md` files, plus historical README notes.

Protected current strong paths
- The active strong model paths remain outside this archive cleanup.
- `RegimeHorizonCostEnsembleModel` active root workflow config is preserved at the Transcendence root.
- `7406` signal plus `CausalRegimeSwitchStrategy` high-signal references remain preserved at the Transcendence root.

Archived experiment families
- Alpha158/Alpha360 regime-horizon runs:
  smoke and full summaries retained for the cost-ensemble baseline family.
- Causal regime / causal signal blends:
  causal residual, tiny-proxy residual, and causal signal regime-blend summaries retained.
- Compact pre-2024 model attempts:
  compact linear rank v2 smoke/full summaries retained.
- Cost-aware survival label attempts:
  smoke summaries retained.
- Deep model checks:
  deep Alpha360 existing transformer smoke/full summaries retained.
- Factor and meta-factor experiments:
  expanded factor, factor-augmented meta, factor-meta fusion lockstep, and factor-meta stability-refit summaries retained.
- Historical ensemble / blend / replay audits:
  base GRU strict blend, historical validation ensemble, long-history signal blend, strict conversion lockstep, and replay splice summaries retained.
- Gate and diagnostics experiments:
  epsilon recheck, robust backtest diagnostics, robust regime lockstep, and 2024->2026 gate summaries retained.
- Portfolio and strategy scans around the 7406 line:
  portfolio innovation, walk-forward, signal-to-portfolio conversion, regime-switch stability, and small walk-forward v23 summaries retained.

Recorded conclusions preserved by retained summaries
- Archived strategy and portfolio scans did not displace the protected 7406 baseline line on strict out-of-sample evidence.
- Walk-forward small scan summaries record that the buffered strategy variant was not robust out of sample.
- Multiple pre-2024 compact / label / residualized variants remained below the hard promotion gates in their retained summaries.
- Summary files remain the canonical historical record for what was run and what verdict each experiment reached.

Cleanup rule applied
- Retained:
  `*_summary_*.json`
  `*_summary_*.md`
  `README*.md`
- Removed:
  non-summary CSV / PKL / JSON intermediate artifacts that duplicated details already referenced by retained summaries
